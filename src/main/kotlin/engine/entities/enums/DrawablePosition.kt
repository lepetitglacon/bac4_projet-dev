package engine.entities.enums

enum class DrawablePosition {
    ABSOLUTE,
    RELATIVE,
    RELATIVE_CENTERED,
    RELATIVE_TO_HERO,
    CENTERED,
    ABSOLUTE_CENTERED,
    X_CENTERED,
    Y_CENTERED
}